{
  "name": "seolleyeon-firestore-rules-tests",
  "private": true,
  "description": "설레연 Firestore/Storage 보안 규칙 테스트 (Firestore + Storage emulator 필요, JDK 21+)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project seolleyeon-rules-test \"node --test\"",
    "test:firestore": "firebase emulators:exec --only firestore --project seolleyeon-rules-test \"node --test blind_meeting_rules.test.js kakao_login_rules.test.js meeting_icebreaker_rules.test.js authz_hardening_rules.test.js\"",
    "test:storage": "firebase emulators:exec --only storage --project seolleyeon-rules-test \"node --test storage_rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.4.0"
  }
}
