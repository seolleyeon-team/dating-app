{
  "name": "seolleyeon-firestore-rules-tests",
  "private": true,
  "description": "블라인드 취향 미팅 Firestore 보안 규칙 테스트 (Firestore emulator 필요)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project seolleyeon-rules-test \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.4.0"
  }
}
