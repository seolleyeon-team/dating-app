{
  "name": "seolleyeon-functions",
  "description": "설레연 Cloud Functions (매치 판정, 알림 등)",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "tsc && node --test \"lib/blindMeeting/__tests__/*.test.js\" \"lib/meetingIcebreaker/__tests__/*.test.js\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}