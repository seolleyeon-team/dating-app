{
  "name": "seolleyeon-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore/Storage security rules tests executed against the Firebase Emulator Suite.",
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 \"*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
