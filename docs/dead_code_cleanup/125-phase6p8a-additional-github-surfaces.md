# Phase 6P-8A — additional GitHub surfaces

| Surface | Read-only result | Content action |
|---|---|---|
| Actions artifacts | 14 metadata records; 0 sensitive-name matches | No downloads |
| Releases/assets | 0 releases; 0 assets | No downloads |
| Deployments | 0 | No content access |
| Pages | 404 / `NO_EVIDENCE` | No content access |
| Packages | API requests returned 401 for queried package types | `UNKNOWN_AUTH_REQUIRED`, not clean/purged |
| PR text/comments/reviews | 0 exact target/blob/old-SHA/email-like matches in scanned text | Raw text not copied |
| PR attachments | `UNKNOWN` | No downloads |

LFS is resolved as `LFS_INVOLVED = NO`: no relevant `.gitattributes` object path and zero `git lfs ls-files --all` entries. No LFS pointer content was read.

The external manifests preserve status codes, counts, and download flags without copying raw content or PII.

